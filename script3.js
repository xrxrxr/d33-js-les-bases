var var_num = prompt("salut petit coquinou combien d'etages veux-tu ?");

function pyramid(var_num) {
if var_num >= 1
	i = 1
	while i <= var_num
		j = 0
		while j < i
    console.log( '#' );
			j+=1
		end
    console.log( "\n" );
		i+=1
}
